# jessjessbot

Discord bot that automatically assigns the "Wordle King" role to daily Wordle winners.

## Setup

1. Install dependencies: `npm install`
2. Add bot token to Railway environment variables
3. Deploy!